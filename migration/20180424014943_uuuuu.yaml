up:
 - CREATE TABLE `clearing_confirm_audit` (
    `clearing_confirm_audit_id` int(11) unsigned NOT NULL AUTO_INCREMENT COMMENT '主键',
    `clearing_confirm_audit_key` varchar(64) NOT NULL COMMENT '流水号，唯一',
    `clearing_confirm_audit_clearing_confirm_id` int(11) unsigned NOT NULL COMMENT '直投资方确认结算id',
    `clearing_confirm_audit_create_user_id` int(11) NOT NULL DEFAULT '0' COMMENT '创建人ID',
    `clearing_confirm_audit_create_user_name` varchar(50) NOT NULL DEFAULT '' COMMENT '创建人姓名',
    `clearing_confirm_audit_audit_user_id` int(11) NOT NULL DEFAULT '0' COMMENT '审核人ID',
    `clearing_confirm_audit_audit_user_name` varchar(50) NOT NULL DEFAULT '' COMMENT '审核人姓名',
    `clearing_confirm_create_at` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `clearing_confirm_update_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (`clearing_confirm_audit_id`),
    KEY `index_audit_key` (`clearing_confirm_audit_key`),
    KEY `index_confirm_id` (`clearing_confirm_audit_clearing_confirm_id`)
  ) ENGINE=InnoDB AUTO_INCREMENT=9 DEFAULT CHARSET=utf8 COMMENT='直投资方结算批量审核中间表';
down:
